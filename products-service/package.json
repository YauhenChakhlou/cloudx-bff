{
  "name": "products-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node run-esbuild.mjs",
    "test": "vitest",
    "test-ui": "vitest --ui",
    "lint": "eslint --ignore-path .gitignore",
    "prettier": "prettier src --write",
    "deploy": "sls deploy",
    "cloudfront:domainInfo": "sls domainInfo",
    "serverless:remove": "sls remove"
  },
  "devDependencies": {
    "@vitest/ui": "^0.30.1",
    "esbuild": "^0.17.16",
    "eslint": "^8.38.0",
    "eslint-config-prettier": "^8.8.0",
    "eslint-plugin-prettier": "^4.2.1",
    "prettier": "^2.8.7",
    "serverless": "^3.30.1",
    "vite": "^4.2.1",
    "vitest": "^0.30.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
